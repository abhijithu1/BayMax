import 'package:flutter/material.dart';

class RelationBody extends StatelessWidget {
  const RelationBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
